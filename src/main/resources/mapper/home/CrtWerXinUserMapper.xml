<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.creatoo.hn.mapper.home.CrtWeiXinMapper">

    <!-- 根据userId查询用户信息 -->
    <select id="findUserInfo4UserId" resultType="hashmap">
        select t.userid,u.`name`,u.isrealname from whg_usr_weixin t,wh_user u where t.userid = u.id 
    </select>

    

</mapper>